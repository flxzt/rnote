This directory contains `.rnote` test files in different versions of the file format to ensure backward compatibility.
**Important**: just recipe `test-file-compatibility` will execute `rnote test` on all `.rnote` files present in this
directory.